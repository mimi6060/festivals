apiVersion: v1
kind: Service
metadata:
  name: festivals-admin
  namespace: festivals
  labels:
    app.kubernetes.io/name: festivals
    app.kubernetes.io/component: admin
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: festivals
    app.kubernetes.io/component: admin
